#include <stdio.h>
void printhw(int count) {
    if (count <= 0) {
        return;
    }
    printf("Hello world\n");
    printhw(count - 1);
}
int main() {
    int times;
    printf("How many times do you want to print? ");
    scanf("%d", &times);
    printhw(times);
    return 0;
}
